import React from 'react';
import { View, StyleSheet, Text, Link } from 'react-native';

const Profile = () => (
    <View >
        <Text >Сторінка особистого профілю</Text>
    </View>
);
const styles = StyleSheet.create({

    Button: {
        flex: 1,
        backgroundColor: '#F0FFF0',
        flexDirection: 'row',
    }
});
export default Profile;