import React from 'react';
import { View, StyleSheet, Text, Link } from 'react-native';
const Settings = () => (
    <View  >
        <Text style={{ textAlign: 'center' }}>Сторінка налаштувань</Text>

    </View>
);
const styles = StyleSheet.create({
    Button: {
    }
});
export default Settings;